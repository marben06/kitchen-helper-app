{
  "name": "kitchen-helper",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "build": "npm install concurrently && cd client && npm install && npm run build"
  },
  "dependencies": {
    "body-parser": "^2.2.2",
    "concurrently": "^9.0.1",
    "express": "^5.2.1",
    "qs": "^6.14.2",
    "react-query": "^3.39.3",
    "vite": "^6.4.1"
  }
}
